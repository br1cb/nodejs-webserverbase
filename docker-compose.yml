version: "3.9"
services:
  mariadb:
    image: mariadb:latest
    container_name: base-mariadb
    environment:
      MARIADB_ROOT_PASSWORD: 1793
    ports:
      - "3306:3306"
    volumes:
      - mariadb_data:/var/lib/mysql

volumes:
  mariadb_data: